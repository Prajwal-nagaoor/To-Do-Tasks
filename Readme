📝 To-Do Application (Django + REST API)

A full-stack To-Do List web application developed using Django and Django REST Framework (DRF) that allows users to efficiently manage daily tasks through a clean, responsive, and interactive interface.
